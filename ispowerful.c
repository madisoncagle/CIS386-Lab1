#include <stdio.h>

// TODO
int main(int argc, char *argv[])
{
    // find every factor between 1 and number
    // then find every prime number out of those

    // loop: square and divide by each
        // if division doesn't yield integer, print false and return 0
    
    // printf("true");

    return 0;
}